# 改造前纸面执行追踪（登记面收窄与域包模板）

假想新域（如 notes：若干纯读/写工具）在改造前的基座上交付，纸面走一遍，
度量三个数。改造收口时以同一假想域重走对照——数字全部实测，出处随行
标注；采集日 2026-08-31，基线是 main @ 0a7cb35 之后的改造前形态。

## 假想域 notes（静态分级工具两枚：list_notes / save_notes）

| 度量 | 改造前实测 | 出处 |
|---|---|---|
| 基座文件编辑数（静态分级工具） | **2** | classifier.py 副作用册加名（两处：纯读/写集合）；builtins.py 顶部 import + build_builtin_tools 装配清单 |
| 基座文件编辑数（条件分级=绑定域工具） | **3** | 上述 2 处 + domain_gate.py 默认名单（绑定域须先入名单，工具模块 import 期 assert 把关）；另 classifier.py 的域名常量 import 也指向新域模块 |
| 为知道"放哪、照谁"要读的代码 | **core/tools/ 平铺 9 文件共 1970 行**（wc -l 实测） | 无目录约定，交付形状靠通读既有代码归纳 |
| 分歧位点数 | **3** | 内联模型：builtins.py（ScheduledTask 模型与任务工具同基座文件）；无模型：feishu_tool.py（无数据形状、无持久化）；手写回执：desk_tool.py（工具内直写 log_event 2 处） |

## 改造后目标（对照基准，收口时以同一假想域实测）

- 静态分级工具：**0** 处基座编辑——域 register().risk 自报，装配期合并名册
- 条件分级工具：**1** 处——core 名册以字符串字面量登记
- 两者各加装配点一行（显式 import + 调用 register()；装配点本职，不计基座编辑）
- 新人只读域模板 ADR（ADR-002）与槽位表即可预测每个文件里有什么、没有什么
- 分歧位点收敛为 1（模板唯一形状）

## 备注

工具集合与分级快照的机器对照在隔壁两个 JSON 夹具
（pre_refactor_tool_set.json / pre_refactor_classification.json），
本文件是人工度量对照——三件合起来构成改造前基线，收口时一并对照。
