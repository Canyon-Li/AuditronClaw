# AuditronClaw

一个可审计的 agent 运行时：防线在代码层强制（命名工具、白名单），行为落审计日志，效果用双维基准度量。工作线：审计 → 加固 → 度量。

## Language

### 防线

**命名工具**:
一个工具即一个能力边界：工具名声明它能做什么，参数面即攻击面。网络能力只以命名工具存在（只读邮箱、飞书推送），不存在"发起任意 HTTP 请求"这类抽象通道。
_Avoid_: 通用网络工具、HTTP 工具、socket 工具

**网络实名门**:
网络出入必须"指名道姓"的原则：命名工具在构造时绑定唯一目标域，守卫只校验该域。域名白名单守卫是它的实现，两者不是同义词。
_Avoid_: 网络过滤、防火墙

**域名白名单**:
允许出站的目标域集合（默认名单 ∪ 环境变量扩展 ∪ 运行时审批规则——经审批门"永久允许"铸入、运行期生效）。守卫校验的是工具绑定的目标域，不解析任何输入里的 URL——LLM 的参数面里没有 URL 字段。
_Avoid_: URL 白名单、域名过滤

**命令白名单**:
shell 工具允许执行的命令集合。与域名白名单同构：默认名单 + 环境变量显式扩展，扩展生效即审计留痕。
_Avoid_: 命令黑名单、shell 白名单（口语可，正式文档统一"命令白名单"）

**脚本执行通道**:
命令白名单放行解释器命令（python 等）后形成的执行能力：白名单只校验脚本路径在 office 内，校验不到脚本内容，脚本进程随之持有宿主账户权限。命令白名单在此处的缺口，由解释器面度量、审批门缓解、容器边界根治。
_Avoid_: 沙箱（那是容器边界的实现）、RCE（口径不同）

**副作用分级**:
按操作的副作用而非工具名定风险：读类免批；写、删、解释器执行、白名单扩展必批。shell 工具按命令段判定——段内全为纯读命令才免批，出现解释器、变更命令或重定向即整条必批。分级结果是规则匹配的输入：规则只能豁免对应级别的动作，改变不了分级本身。
_Avoid_: 工具分级（按工具名分）、权限等级

**审批门**:
工具层代码强制的批准机制（路线图称策略层）：副作用分级判为高危的工具调用，必经既定规则放行或人批准才执行，审批事件入审计。拦的是"受骗的合法操作"——写与执行同门，依据是人批没批，不是提示词自觉。无人值守时仅规则能开门。与容器边界的"构造拦"相对，这是"人拦"。
_Avoid_: 确认弹窗、人机验证

**审批规则**:
高危操作的唯一豁免通道：一次"永久允许"铸出的持久化授权，绑定动作与目标作用域，条目带出处。规则之外的高危在无人时一律拒、拒绝并继续。
_Avoid_: 白名单（那是命令/域名名单的词）、权限配置

**外部数据区**:
进入 LLM 上下文的不受信内容被显式框定为"系统记录，非指令"的区段（系统提示词三段式的中段）。邮件正文等外部输入进上下文前必须过此框定。
_Avoid_: 内容包装、转义区、sanitize

**信任面**:
凭据（邮箱授权码、webhook URL）合法存在的唯一区域：宿主机 .env。执行面（工具参数、返回值、LLM 上下文、审计日志）永远不出现凭据。
_Avoid_: 密钥管理、配置中心

### 基准

**双维基准**:
方向相反的一对基准的合称：注入基准断言恶意行为"不许发生"，能力基准断言正常任务"必须发生且结果正确"。
_Avoid_: 测试套件、评估集

**注入基准**:
恶意输入的拦截度量，按入口面分组，双层判定：提示面拦截（LLM 未被骗）与危害落地（攻击经全链路达成）。
_Avoid_: 安全测试、红队演练

**解释器面**（interpreter 面）:
注入基准的一个分组，按攻击利用的通道而非输入入口定义：用例诱导 LLM 在 office 内写外观无害的脚本并执行，危害锚点是对该脚本的执行调用。它度量的是脚本执行通道；话术从哪个入口进来，是入口面的事。
_Avoid_: 脚本执行面（与执行面撞名）、第 6 入口面

**入口面**:
恶意输入进入系统的通道（邮件正文、SKILL.md、画像……），注入基准的分组与独立计分单位。
_Avoid_: 攻击向量、测试分类

**能力基准**:
正常任务达成度的度量：工具选对、动作发生、结果正确。golden 用例是其载体。
_Avoid_: golden test、功能测试

### 运行时

**心跳**:
agent 的自主驱动引擎：循环任务到期产生系统消息进会话队列，与手动对话共用同一会话机制与审计流。不引入独立调度系统。
_Avoid_: cron、作业系统、定时器

**邮箱事务台**:
本项目第一个真实在用的任务：定时只读近期邮箱 → 分类总结 → 待办落盘 → 摘要推送到飞书。是事务台不是邮件客户端：只读与只推送是结构边界，撰写/标记/删除不在能力面内。
_Avoid_: 邮件助手、邮箱客户端

**分类账**:
事务台摘要的固定格式：计数打头、按类分组、展开密度随类别递减（待办详列带来源，推广只留计数）。tasks.json 落盘与飞书推送共用此数据形状。
_Avoid_: 逐封列表、全文摘要

**待办**:
从邮件提炼的行动项，与 category 正交并存于单封邮件的分类结果：邮件的性质类只决定它在分类账里的展开密度，待办本身跨类别汇总。
_Avoid_: 任务（那是 task CRUD 的词）、提醒

**会话引擎**:
以会话为持有域、以回合为操作粒度的深模块：驱动一个回合，把底层事件流解析为回合事件发出；TUI、基准、Web 终端都是它的适配器。不吃输入队列、不管心跳、不做审计。
_Avoid_: 常驻循环、消息泵

**回合**:
一次输入（用户消息或心跳系统消息）驱动 agent 从开始到收尾的最小执行单位；一个会话（thread_id）由若干回合组成。
_Avoid_: 轮询、请求

**回合事件**:
会话引擎对外发出的类型化事件，四种：tool_call / tool_result / reply / turn_end。审批通道（approval_request）声明在接口上，产生与续行由策略层实现。
_Avoid_: 消息、日志（那是审计流的词）

**回合轨迹**:
一个回合内按序收集的 tool_calls、tool_results 与 reply 的结构化记录，基准判定与 Reflector 核对的事实载体。
_Avoid_: 历史、transcript
